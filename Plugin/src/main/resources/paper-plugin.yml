name: "LibreLogin"
author: "kyngs"
version: '${version}'
main: "win.sectorfive.viflcraftauth.paper.PaperBootstrap"
api-version: "1.19"

dependencies:
  server:
    Geyser-Spigot:
      load: "BEFORE"
      required: false
    floodgate:
      load: "BEFORE"
      required: false
    LuckPerms:
      load: "BEFORE"
      required: false